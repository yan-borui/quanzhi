# 角色技能实际逻辑表

> 本文以当前 Python 实现为准。CD N 表示使用后完整间隔 N 个回合，即在第 R 回合使用 CD 1，到第 R+2 回合（例如第 1 回合使用、第 3 回合）才能再次使用。该语义由 `Skill.start_cooldown()` 统一实现；表中的“配置 CD”现与此定义一致。

## 全局规则与高风险差异

| 项目 | 当前实际逻辑 |
|---|---|
| 控制 | 除风阵、火阵、飞镰、护盾外，已登记控制都会令角色不能行动，只能解控；未登记名称默认也是阻断行动。多数控制可叠层，但解控流程需结合后端确认每次清除量。 |
| 距离 | `is_nearby` 仅比较双方 `block_id`。除后端专门筛选的技能外，角色技能函数自身通常不校验射程。 |
| 技能失败 | 效果函数返回 `False` 时不进入冷却；前置条件失败也不消耗冷却。 |
| CD 时序 | 成功施放后，`start_cooldown()` 保存 N 并标记“刚启动”；下回合开始只清除该标记，不递减，之后每回合开始减 1。因此存储/展示值始终与声明一致，CD N 最早在第 R+N+1 回合可用，CD 0 则在下一回合可用。 |
| 专用技能 | 回旋斩、光盘散落、忍者偷袭后的地心冷却、术士死亡之门等手动启动冷却的路径也统一使用 `start_cooldown()`。`set_cooldown()` 仅保留给直接设置内部剩余计数的测试和管理逻辑。 |
| 展示数据 | 多个 `*_SKILLS_DATA` 的 CD 与构造值不一致。下表“声明差异”专门标出。 |
| 目标 | 多数 `use_skill()` 会把自己作为目标；真正对战通常由后端选择目标并调用 `use_skill_on_target()`。 |
| 死亡清理 | 任意死亡路径都会先清除角色身上的全部控制，再执行角色特有的死亡、复活或转移逻辑。 |
| 立盾 | 治疗师给任意角色施加的立盾都具有可叠加的独立 HP；整次攻击由立盾承担，击破时溢出伤害不传给本体。 |

## 骑士（60 HP）

| 技能 | 配置 CD | 目标/条件 | 当前实际效果 | 声明差异与注意点 |
|---|---:|---|---|---|
| 无畏冲锋 | 2 | 单体 | 造成 12 伤害。 | 代码不校验距离，也没有冲锋位移或控制。 |
| 斩 | 0 | 单体 | 造成 3 伤害。 | 数据表标为近程，但技能函数不校验近程。 |
| 盾 | 0 | 自身；每局 5 次；`current_round>=2`，且历史中的上一回合存活、无控制 | 返还上一回合记录的伤害，撤销上一回合新增控制和印记，并清空该条记录；不回退位置、治疗、资源或修正。死亡/受控时也可由后端暴露入口。 | 可用性实际上由历史状态判断，不直接使用已定义的死亡/受控窗口；即使无效果也耗次数并启动 CD 0（本回合不可重复，下回合恢复）。 |

## 召唤师（60 HP）

| 技能 | 配置 CD | 目标/条件 | 当前实际效果 | 注意点 |
|---|---:|---|---|---|
| 狼 | 0 | 自己或单体 | 对自己：狼资源 +2；对他人：狼 +1，并给目标易伤 +20%。 | 易伤可叠，但在目标下一次受伤时全部消耗，并非严格“下一回合”。 |
| 熊 | 0 | 自己或单体 | 对自己：熊资源 +2；对他人：熊 +1，并给目标攻击强化 +6。 | 强化可叠，在目标下一次造成经 `apply_attack_buff` 处理的伤害时全部消耗。 |
| 齐攻 | 0 | 狼或熊至少 6；单体 | 优先消耗 6 狼，否则消耗 6 熊，造成 30 伤害。 | “群体攻击”描述与实现矛盾：实际仅攻击一个目标；两种资源都够时固定消耗狼。 |

## 剑客（60 HP）

| 技能 | 配置 CD | 目标/条件 | 当前实际效果 | 声明差异与注意点 |
|---|---:|---|---|---|
| 游刃斩 | 1 | 单体 | 6 伤害，目标剑意 +1。 | 代码不校验距离。 |
| 回旋斩 | 1 | 后端选择与剑客同地块的所有非自身目标 | 每个目标受到同一个计算后的 6 伤害并获剑意 +1。 | 攻击强化只计算/消耗一次，但增强后的数值应用给所有目标。单体入口恒失败。 |
| 闪电劈 | 0 | 目标剑意至少 3 | 添加一层 `lightning_strike` 阻断控制；不造成伤害。 | 不消耗剑意，因此可继续作为条件使用。 |
| 无敌刺 | 0 | 目标有 `lightning_strike`；每局对同一对象最多一次 | 造成 42 伤害，记录 Python 对象 ID。 | 不移除闪电劈控制；“每局”依赖后端调用重置。 |

## 阵鬼（60 HP）

| 技能 | 配置 CD | 目标/条件 | 当前实际效果 | 注意点 |
|---|---:|---|---|---|
| 瘟阵 | 0 | 目标尚无瘟阵 | 添加 1 层瘟阵，阻断目标行动。 | 除通用控制外没有“瘟疫”附加效果，不可叠。 |
| 灰阵 | 0 | 目标尚无灰阵 | 添加 1 层灰阵，阻断目标行动。 | 除通用控制外没有额外效果，不可叠。 |
| 风阵 | 0 | 目标尚无风阵 | 添加非阻断风阵标记。 | 注释称禁止近程技能，但角色类和状态定义均未执行该限制；需依赖后端，当前未见对应筛选。 |
| 火阵 | 2 | 目标尚无火阵 | 添加非阻断火阵；目标每次 `on_turn_start` 受到 `2×层数` 伤害。 | 不可叠，所以通常每回合 2；无持续时间，不会自然消失。 |
| 五彩法阵 | 0 | 目标同时有四阵；双人判定由施法者获胜 | 造成 60 伤害。 | 判定平局或目标获胜均失败且不进 CD；成功后不消耗/清除四阵。 |

## 治疗师（30 HP）

| 技能 | 配置 CD | 目标/条件 | 当前实际效果 | 声明差异与注意点 |
|---|---:|---|---|---|
| 大血包 | 2 | 单体 | 治疗 12；对自己使用时减伤回合 +2，上限 2。 | 减伤在自身回合开始先减 1，因此持续时序可能短于直觉。 |
| 小血包 | 1 | 单体 | 治疗 6；对自己使用时减伤回合 +1，上限 2。 | 受减伤保护时伤害减半向下取整、最低 1。 |
| 套盾 | 1 | 单体 | 对治疗师：创建 3 HP 立盾，已有盾则 +3，破碎时清除持盾期间新获的阻断控制。对其他角色：立盾 HP +3。所有立盾均承受整次攻击且溢出不传本体。 | 非治疗师（包括机器人模式的科学家）现通过通用受伤流程正常享受立盾抗伤；非治疗师盾碎不附带治疗师专属解控。 |
| 大火球 | 1 | 单体 | 造成 6 伤害。 | 无额外效果。 |

## 游侠（60 HP）

| 技能 | 配置 CD | 目标/条件 | 当前实际效果 | 注意点 |
|---|---:|---|---|---|
| 板砖 | 0 | 单体 | 同地块基础 6，否则 3；若 `current_round` 连续且目标为同一对象，连击次数每次 +1，每层额外 +3。 | 中断后重置；若外部未更新 `current_round`，连击判断可能异常。无伤害上限。 |
| 纱袋 | 1 | 目标尚无纱袋 | 添加 1 层阻断控制，并把目标 `block_id` 直接设为游侠所在地块。 | 不可叠；直接改地块，未在技能内同步其他邻接状态。 |

## 魔道学者（60 HP）

| 技能 | 配置 CD | 目标/条件 | 当前实际效果 | 注意点 |
|---|---:|---|---|---|
| 星星射线 | 1 | 单体 | 造成 6 伤害。 | 无额外效果。 |
| 燃烧瓶 | 1 | 选定一个角色以确定其当前地块 | 在该地块增加一个永久持续效果；系统每次触发时对地块内角色造成 3 伤害，可叠加、可友伤。 | 效果绑定地块而非目标；持续时间 `-1`，没有自然结束条件。 |

## 卖油翁（60 HP）

| 技能 | 配置 CD | 目标/条件 | 当前实际效果 | 注意点 |
|---|---:|---|---|---|
| 铜孔倒油 | 1 | 单体 | 造成 6 伤害。 | 无油资源交互。 |
| 教训你 | 2 | 单体 | 6 伤害并添加 1 层阻断控制。 | 控制可叠。 |
| 一锅油 | 3 | 自身瞬发 | 油锅计数 +1。 | 初始油锅计数为 0；“瞬发”只代表忽略目标，是否消耗行动由后端决定。 |
| 倒你脸上 | 0 | 油锅计数至少 1；每个自身回合最多一次 | 消耗 1 油锅，造成 15 伤害。 | 每回合限制只在技能成功后扣除；另有非技能 `drink_oil` 可让任意角色消耗油锅并治疗 3。 |

## 机械师（60 HP）

| 技能 | 配置 CD | 目标/条件 | 当前实际效果 | 注意点 |
|---|---:|---|---|---|
| 机械空投 | 2 | 单体 | 12 伤害。 | 无机械实体或范围效果。 |
| 机械追踪 | 1 | 单体 | 6 伤害。 | 没有追踪、必中或隐身穿透逻辑。 |
| 跳雷 | 1 | 单体 | 6 伤害。 | 没有布雷、延迟触发或位移联动。 |
| 电子眼 | 0 | 目标当前 `stealth>0` | 若目标实现 `be_found_directly` 则调用，否则直接将 `stealth=0`。 | 未隐身时失败；它本身直接找出目标，不是给后续“找”增加能力。 |

## 吃鸡大师（60 HP）

| 技能 | 配置 CD | 目标/条件 | 当前实际效果 | 注意点 |
|---|---:|---|---|---|
| 落地成盒 | 2 | 单体 | 12 伤害。 | 无额外效果。 |
| M4扫射 | 1 | 单体 | 6 伤害。 | 实际不是多段或范围。 |
| 烟雾弹 | 2 | 单体 | 添加 1 层烟雾弹阻断控制。 | 可叠；不是隐身或视野效果。 |
| 空投 | 0 | 无实质目标 | 自身空投资源 +1。 | 死亡时若空投 `n>=4`，消耗全部并待复活为 `3n` HP，同时把本局最大 HP 改为 `3n`；新局重置恢复 60/60 HP。 |

## 卖光盘的（60 HP）

| 技能 | 配置 CD | 目标/条件 | 当前实际效果 | 注意点 |
|---|---:|---|---|---|
| 光盘 | 0 | 单体 | 3 伤害，自身光盘 +1。 | 即使攻击自己也会获得资源。 |
| 亮瞎你 | 0 | 至少 3 光盘；单体 | 先消耗 3 光盘，再造成 15 伤害并加 1 层阻断控制。 | 控制可叠。 |
| 光盘飞刀 | 0 | 至少 5 光盘；单体 | 先消耗 5 光盘，再造成 30 伤害。 | 无飞行/范围逻辑。 |
| 光盘散落 | 2 | 后端入口：在场玩家数大于 0 | 获得等于在场玩家数的光盘。 | 普通单体入口只获得 1 张；正确效果依赖后端调用专用方法。 |

## 忍者（60 HP）

| 技能 | 配置 CD | 目标/条件 | 当前实际效果 | 注意点 |
|---|---:|---|---|---|
| 樱花岁月 | 2 | 单体 | 先解除自己建立的铁索绑定，再造成 12 伤害。 | 解除的是忍者作为来源的绑定，不是自身受到的铁索。 |
| 铁索覆身 | 0 | 单体 | 建立唯一绑定并给目标 1 层阻断控制；改绑时自动清除旧目标铁索。 | 可对自己使用；目标自行解控后需后端通知忍者才能同步解绑。 |
| 摔 | 0 | 必须是当前铁索绑定目标且仍有铁索控制 | 造成 6 伤害。 | 不消耗或解除铁索，可反复使用。 |
| 忍法地心 | 2 | 无实质目标 | 解除自己建立的铁索绑定，清除火阵等角色持续伤害和持续效果，随机移动到当前未占用的新地块以脱离燃烧瓶等地块伤害，再设置隐身。 | 受任意正伤害即脱离；搜索者双人判定获胜或电子眼可找出。旧地块的燃烧瓶仍存在，只是不再影响已离开的忍者。 |
| 偷袭 | 0 | 忍者处于内部隐身状态；单体 | 造成 6 伤害，并把忍法地心 CD 强制设为 2。 | 不会退出隐身；注释称樱花岁月也进入 CD，但实现只设置忍法地心。 |

## 镰刀工（60 HP）

| 技能 | 配置 CD | 目标/条件 | 当前实际效果 | 注意点 |
|---|---:|---|---|---|
| 飞镰 | 0 | 单体 | 唯一绑定目标并添加非阻断“飞镰”标记；改绑清旧目标。镰刀工死亡时若绑定目标存活，直接令其 HP=0 并触发死亡。 | 可解控移除；可绑定自己，存在死亡递归/自带走风险。 |
| 飞镰斩 | 1 | 目标有飞镰标记 | 9 伤害，并记录为“本回合命中目标”。 | 描述菜单只看任意飞镰标记，不确认标记是否来自当前镰刀工。 |
| 黑暗飞镰 | 2 | 只能对上个自身回合由飞镰斩记录的同一对象 | 12 伤害，成功后清除联动目标。 | 每次回合开始会用 `_slash_target_pending` 覆盖可用目标；错过窗口即丢失。 |
| 挥镰 | 2 | 目标不在当前挥镰追踪集合 | 先造成 6 伤害，再建立唯一挥镰绑定并添加阻断控制；改绑清旧目标。 | 目标解控后需后端通知才能正确解除追踪限制。 |

## 科学家（60 HP）

| 技能 | 配置 CD | 目标/条件 | 当前实际效果 | 注意点 |
|---|---:|---|---|---|
| 高压电池 | 2 | 单体 | 12 伤害并加 1 层阻断控制。 | 控制可叠。 |
| 燃烧电池 | 1 | 单体 | 6 伤害。 | 名称虽为燃烧，但无持续伤害。 |
| 电池 | 0 | 无实质目标 | 自身电池 +1。 | 无上限。 |
| 制造机器人 | 0 | 至少 4 电池 | 消耗 4 电池，创建一个 1 HP、可被选为目标的小机器人。 | 机器人不参与判定且不能行动；名称由外部预设，否则自动生成。 |
| 撸 | 0 | 至少 1 个存活机器人；单体 | 造成 `3×存活机器人数` 伤害。 | 不消耗机器人。 |
| 机器人自爆 | 0 | 至少 1 个存活机器人；单体 | 造成 `15×存活机器人数` 伤害，并将所有机器人 HP 设 0。 | 仅单体。机器人模式中使用后科学家真正死亡。 |
| 死亡被动 | — | 科学家死亡时仍有机器人 | 科学家改为 1/1 HP 并进入机器人模式，只能用“撸/自爆”；此后每次未被护盾吸收的正伤害固定损失一个机器人，不看伤害数值。 | 机器人全灭后真正死亡；新局重置恢复 60/60 HP。 |

## 术士（60 HP）

| 技能 | 配置 CD | 目标/条件 | 当前实际效果 | 注意点 |
|---|---:|---|---|---|
| 诅咒之剑 | 1 | 单体 | 6 伤害。 | 没有诅咒效果。 |
| 混沌之雨 | 1 | 单体 | 6 伤害。 | 实际不是范围或持续技能。 |
| 死亡之门 | 2（延后设置） | 后端多选目标；场上无本术士激活的死亡之门 | 给所有所选目标各加 1 层阻断控制，记录初始人数；施放时不进入 CD，全部解除或爆炸后才设 CD 2。 | 普通单体入口会直接加控制并立即按常规进入 CD，但不设置 `_death_gate_active`，两条路径语义不同。 |
| 爆炸 | 0 | 后端锁定所有仍有死亡之门的目标 | 每目标基础伤害为 `max(1, 12//初始分门人数)`；逐个伤害并解除死亡之门，最后关闭门并令死亡之门 CD=2。 | 整除会损失余数，故总伤害可能小于 12；攻击强化对“每人伤害”加一次后复制给所有目标。普通单体入口则直接造成 12。 |
| 六星法阵 | 2 | 单体 | 添加 1 层阻断控制。 | 无六星组合、范围或额外法阵逻辑。 |

## 靶子（60 HP）

| 技能 | 配置 CD | 目标/条件 | 当前实际效果 | 注意点 |
|---|---:|---|---|---|
| 平A | 0 | 单体 | 造成 6 伤害。 | 会正常应用靶子自身的攻击强化；代码不校验距离。 |

## 建议的修改核对顺序

1. CD 时序现已统一；后续新增技能应直接填写设计 CD，并通过 `start_cooldown()` 启动冷却，不再手工加 1。
2. 再确认射程、目标数量、是否允许对自己使用，以及控制是否可叠/持续多久。
3. 然后处理高风险状态机：骑士盾、忍者隐身、镰刀绑定、科学家死亡转移、术士多目标门。
4. 最后统一资源、强化、易伤、护盾和持续效果的消耗时机，并为每项预期补回归测试。
